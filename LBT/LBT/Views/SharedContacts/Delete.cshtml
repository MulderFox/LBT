@using LBT.Resources
@model LBT.ModelViews.SharedContactsDelete

@{
    ViewBag.Title = ViewResource.SharedContacts_Delete_Title_Text;
}

<p>@ViewResource.Global_DeleteConfirmationQuestion_Text</p>

<section id="deleteForm">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary()
    
        <fieldset>
            <legend>@ViewResource.Global_SharedContactsLegend_Title</legend>
            <table>
                <tbody>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.ToUser.LyonessId):
                        </th>
                        <td>
                            @Html.DisplayFor(model => model.ToUser.LyonessId)
                        </td>
                    </tr>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.ToUser.LastName):
                        </th>
                        <td>
                            @Html.DisplayFor(model => model.ToUser.LastName)
                        </td>
                    </tr>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.ToUser.FirstName):
                        </th>
                        <td>
                            @Html.DisplayFor(model => model.ToUser.FirstName)
                        </td>
                    </tr>
                </tbody>
            </table>
            <input type="submit" autofocus value="@ViewResource.Global_HplDeleteTitle_Text" />
        </fieldset>
    }
</section>

@section navigation2
{
    <div id="navigation2">
        <div class="content-wrapper">
            <div class="float-left">
                <nav>
                    <ul class="menu2">
                        <li>@Html.ActionLink(ViewResource.Global_HplBackToSharedContactIndex_Text, "Index")</li>
                    </ul>
                </nav>
            </div>
        </div>
    </div>
}
