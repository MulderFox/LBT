using System.Reflection;
using LBT.Services.ffMpeg;
using NUnit.Framework;
using System.IO;

namespace LBT.Tests.Services.ffMpeg
{
    [TestFixture]
    public class ConverterTests
    {
        [Test]
        public void GetVideoInfoTest()
        {
            //VideoFile videoFile;
            //using (var ms = new MemoryStream(Properties.Resources.mov_bbb))
            //{
            //    var converter = new Converter(Assembly.GetExecutingAssembly().Location);
            //    videoFile = converter.GetVideoInfo(ms, "mov_bbb.mp4");
            //}

            //Assert.IsNotNull(videoFile);
        }
    }
}
